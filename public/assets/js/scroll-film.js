(() => {
  'use strict';
  const film=document.querySelector('.scroll-film');
  if(!film)return;
  const stage=film.querySelector('.scroll-film__stage'),canvas=film.querySelector('.scroll-film__canvas'),fx=film.querySelector('.scroll-film__fx'),ctx=canvas?.getContext('2d',{alpha:false}),fctx=fx?.getContext('2d'),progress=film.querySelector('.scroll-film__progress'),counter=film.querySelector('.scroll-film__counter'),title=film.querySelector('.scroll-film__title'),copy=film.querySelector('.scroll-film__copy'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
  const frames=[
   {title:'Замысел',copy:'Начинаем с задачи, ритма жизни и архитектуры пространства.'},
   {title:'Подготовка',copy:'Выверяем геометрию, инженерные решения и будущую смету.'},
   {title:'Работа',copy:'Преобразуем пространство поэтапно — с контролем деталей.'},
   {title:'Финиш',copy:'Передаём интерьер, в котором всё на своём месте.'}
  ];
  let raf=0,video=null,currentP=0,lastStep=-1;
  const resizeCanvas=(el)=>{if(!el)return;const dpr=Math.min(devicePixelRatio||1,2),w=el.clientWidth||innerWidth,h=el.clientHeight||innerHeight,cw=Math.round(w*dpr),ch=Math.round(h*dpr);if(el.width!==cw||el.height!==ch){el.width=cw;el.height=ch}return [w,h,dpr]};
  const draw=()=>{if(!ctx||!canvas||!video||video.readyState<2)return;const [w,h,dpr]=resizeCanvas(canvas);ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);const scale=Math.max(w/720*(innerWidth<=700?1.03:1.16),h/1280*(innerWidth<=700?1.18:1.04)),dw=720*scale,dh=1280*scale;ctx.drawImage(video,(w-dw)/2,(h-dh)/2,dw,dh)};
  const drawFx=(p)=>{if(!fctx||!fx)return;const [w,h,dpr]=resizeCanvas(fx);fctx.setTransform(dpr,0,0,dpr,0,0);fctx.clearRect(0,0,w,h);const cx=w*.74,cy=h*.38,shift=p*70,g=fctx.createRadialGradient(cx-shift,cy,0,cx-shift,cy,Math.max(w,h)*.45);g.addColorStop(0,'rgba(224,189,111,.24)');g.addColorStop(.16,'rgba(224,189,111,.08)');g.addColorStop(.5,'rgba(224,189,111,0)');fctx.fillStyle=g;fctx.fillRect(0,0,w,h);fctx.save();fctx.translate(w*.74,h*.38);fctx.rotate(-.35+p*.18);fctx.strokeStyle='rgba(228,193,119,.26)';fctx.lineWidth=1;for(let i=0;i<5;i++){const r=60+i*42+p*75;fctx.beginPath();fctx.arc(0,0,r,0,Math.PI*1.55);fctx.stroke()}fctx.restore()};
  const update=()=>{raf=0;const rect=stage.getBoundingClientRect(),max=Math.max(1,stage.offsetHeight-innerHeight),p=Math.max(0,Math.min(1,-rect.top/max)),i=Math.min(frames.length-1,Math.floor(p*frames.length));currentP=p;stage.dataset.step=String(i);stage.style.setProperty('--story-shift-x',(p*78).toFixed(1)+'px');stage.style.setProperty('--story-shift-y',(p*-24).toFixed(1)+'px');stage.style.setProperty('--story-scale',(0.82+p*.2).toFixed(3));stage.style.setProperty('--story-rotate-x',(5+p*5).toFixed(2)+'deg');stage.style.setProperty('--story-rotate-y',(-14+p*16).toFixed(2)+'deg');stage.style.setProperty('--story-rotate-z',(-3+p*7).toFixed(2)+'deg');stage.style.setProperty('--room-rotate-x',(58-p*18).toFixed(2)+'deg');stage.style.setProperty('--room-rotate-z',(-9+p*5).toFixed(2)+'deg');if(progress)progress.style.width=p*100+'%';if(counter)counter.textContent=String(i+1).padStart(2,'0')+' / 04';if(i!==lastStep){if(title)title.textContent=frames[i].title;if(copy)copy.textContent=frames[i].copy;lastStep=i}if(video&&video.readyState>=1&&Number.isFinite(video.duration)&&video.duration>0){const next=video.duration*p;if(Math.abs(video.currentTime-next)>.035){try{video.currentTime=next}catch(_){}}else draw()}drawFx(p)};
  const request=()=>{if(!raf)raf=requestAnimationFrame(update)};addEventListener('scroll',request,{passive:true});addEventListener('resize',()=>{resizeCanvas(canvas);resizeCanvas(fx);request()},{passive:true});
  if(reduce){stage.dataset.step='0';if(progress)progress.style.width='0%';return}
  video=document.createElement('video');video.muted=true;video.playsInline=true;video.preload='auto';video.src='assets/video/hero.mp4';video.setAttribute('aria-hidden','true');video.style.display='none';video.addEventListener('loadedmetadata',()=>{video.currentTime=0;draw();request()});video.addEventListener('seeked',()=>{draw();drawFx(currentP)});video.addEventListener('error',()=>canvas?.classList.add('is-fallback'));video.load();request();
})();
