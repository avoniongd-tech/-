(()=>{const house=document.querySelector('[data-house]');let down=false,last=0,ry=-25,rx=58;function move(x){if(!down)return;ry+=(x-last)*.35;last=x;house.querySelector('.house-shell').style.transform=`translate(-38%,-50%) rotateX(${rx}deg) rotateZ(-7deg) rotateY(${ry}deg)`}house.addEventListener('pointerdown',e=>{down=true;last=e.clientX;house.classList.add('drag');house.setPointerCapture(e.pointerId)});['pointerup','pointercancel'].forEach(x=>house.addEventListener(x,()=>{down=false;house.classList.remove('drag')}));house.addEventListener('pointermove',e=>move(e.clientX));document.querySelector('[data-enter]').addEventListener('click',()=>document.querySelector('#rooms').scrollIntoView({behavior:'smooth'}));document.querySelectorAll('.room-card').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.room-card').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));const menu=document.querySelector('.menu');menu.addEventListener('click',()=>{const nav=document.querySelector('.topbar nav');nav.classList.toggle('open')});})();